import styled from "styled-components";
import {fonts} from "../../../app/styles/mixins/mixins";

export const BlockNotFound = styled('div')`
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100%;
    
    ${fonts.middle}
`

