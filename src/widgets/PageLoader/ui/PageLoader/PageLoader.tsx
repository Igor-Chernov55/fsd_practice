import {BlockPageLoader} from "./PageLoader.styled";

interface PageLoaderProps {

}

export const PageLoader = () => {
    return (
        <BlockPageLoader>
            <PageLoader />
        </BlockPageLoader>
    );
};
